node_modules/
dist/
.DS_Store
*.log
.env
.env.*
node_modules
.aider*
node-v*-darwin-*/

# Swift / Xcode build artifacts (native/ has its own .gitignore for .build/,
# these cover the same tooling if it's ever invoked from the repo root or a
# future Xcode project is added)
.build/
.swiftpm/
DerivedData/
*.xcuserstate
xcuserdata/

# Stray dev-server output that shouldn't be version controlled
vite-out.txt
vite-dev.log
